<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="ChangeRequestDialog"
        title="Change Request"
    >
        <VBox class="sapUiSmallMargin">
            <!-- Number -->
            <Label text="Number" required="true"/>
            <Input
                id="crNumber"
                type="Number"
                editable="{= ${ChangeRequestModel>/isEditable} }"
                value="{ChangeRequestModel>/number}"
            />

            <!-- Change Request -->
            <Label text="Change Request" required="true"/>
            <Input
                id="crChangeRequest"
                value="{ChangeRequestModel>/changeRequest}"
            />

            <!-- Type -->
            <Label text="Type" required="true"/>
            <Select
                id="crType"
                selectedKey="{ChangeRequestModel>/type}"
                width="100%"
            >
                <core:Item
                    key=""
                    text="-- Select Type --"
                />
                <core:Item
                    key="Planning"
                    text="Planning"
                />
                <core:Item
                    key="Implementation"
                    text="Implementation"
                />
                <core:Item
                    key="Testing"
                    text="Testing"
                />
                <core:Item
                    key="Review"
                    text="Review"
                />
            </Select>

            <!-- Status (Hardcoded) -->
            <Label text="Status" required="true"/>
            <Select
                id="crStatus"
                selectedKey="{ChangeRequestModel>/status}"
                width="100%"
            >
                <core:Item
                    key=""
                    text="-- Select Status --"
                />
                <core:Item
                    key="Pending"
                    text="Pending"
                />
                <core:Item
                    key="Open"
                    text="Open"
                />
                <core:Item
                    key="InProgress"
                    text="InProgress"
                />
                <core:Item
                    key="Closed"
                    text="Closed"
                />
                <core:Item
                    key="Canceled"
                    text="Canceled"
                />
            </Select>
            <!-- BA (Input instead of dropdown) -->
            <Label text="BA" required="true"/>
            <Input
                id="crBA"
                value="{ChangeRequestModel>/BA}"
            />

            <!-- Assigned To (Input instead of dropdown) -->
            <Label text="Assigned To" required="true"/>
            <Input
                id="crAssignedTo"
                value="{ChangeRequestModel>/assignedTo}"
            />

            <!-- Description -->
            <Label text="Description" required="true"/>
            <TextArea
                id="crDescription"
                value="{ChangeRequestModel>/description}"
                rows="1"
                width="100%"
            />
            <Label text="Created Date" />
            <DatePicker
                id="crCreateDate"
                value="{ChangeRequestModel>/createDate}"
                displayFormat="yyyy-MM-dd"
                valueFormat="yyyy-MM-dd"
            />

            <!-- On Hold (Radio Buttons) -->
            <CheckBox
                id="crOnHold"
                text="On Hold"
                selected="{ChangeRequestModel>/onHoldcheck}"
            />

            <!-- Vistex Check (Horizontal Radio Buttons) -->
            <Label text="Vistex Check" />
            <RadioButtonGroup
                id="crVistex"
                selectedIndex="{ChangeRequestModel>/vistexcheck}"
                columns="2"
            >
                <buttons>
                    <RadioButton
                        text="Yes"
                        
                    />
                    <RadioButton
                        text="No"
                      
                    />
                </buttons>
            </RadioButtonGroup>
        </VBox>

        <beginButton>
            <Button
                text="Save"
                press="onCreateupdatePress"
            />
        </beginButton>
        <endButton>
            <Button
                text="Cancel"
                press="onCancelPress"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
